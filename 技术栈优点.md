# 知识星图 · 技术选型优点

> 本项目是一个**本地优先的个人知识图谱 + 跨文档证据问答**应用：单机即可完整运行，支持多账号数据隔离，没配密钥也能把全链路走通。技术选型整体偏向「轻量、少依赖、可离线兜底」。本文按层说明所用技术与优点。

## 一、前端

- **React 18 + Hooks**

  - 12 个组件拼装界面：`GraphView` / `EntityDetail` / `ReviewPanel` / `DocumentPanel` / `QAPanel` / `Dashboard` / `SpaceBar` / `SettingsPanel` / `AuthScreen` / `AuthPanel` / `Confirm` / `Toast`；`App.jsx`（约 900 行）只负责页面装配与顶部状态。
  - 状态用 `useState / useEffect / useMemo / useCallback / useRef` 管理，**没有引入状态库**：这个应用的共享状态基本都落在后端与 `localStorage`，多一层 store 只会多一层间接。

- **Vite 5**

  - 冷启动与 HMR 快，`@vitejs/plugin-react` 提供 Fast Refresh，改组件/样式即时可见。
  - `vite.config.js` 把 `/api` 代理到后端，**前端代码里不出现任何后端地址**，开发期连 CORS 都不用配；构建产物是纯静态文件，随便丢给任意静态服务器即可。
  - 重依赖用**动态 `import()` 做代码分割**：`cytoscape-elk` 只在切到 ELK 布局时才加载（`ensureElk()`），首屏不必为它买单。

- **Cytoscape.js 3.30 + fCoSE / ELK**

  - 专为图结构设计的可视化库，布局、缩放平移、节点拖拽、样式引擎全部自带，比 canvas/SVG 自绘省事且性能更好——正好契合「知识星图」。
  - 用 `cytoscape-fcose`（cose 的加速增强版）作默认力导向布局，用 `cytoscape-elk` 的 layered 算法减少有向边交叉；7 种布局在代码里只是 `buildLayout(name)` 多一个分支。
  - 样式集中由 `buildStyle(theme, { colorByType, showEdgeLabels })` 生成，明暗两套配色、边标签与按类型着色开关都收敛在一处。

- **原生 CSS + CSS 变量**

  - 1458 行 `styles.css`、零额外依赖；`applyTheme` 只改 `<html data-theme>`，全部配色由变量级联切换，**不用为暗色模式维护第二套样式表**。

- **localStorage 偏好层（`prefs.js`）**

  - 主题、布局、筛选、边标签等视图偏好存在本地：不走服务器、无需登录；读取失败一律回退默认值，隐私模式下不可写也不影响主流程。

## 二、后端

- **FastAPI + Uvicorn**

  - 基于类型注解的 ASGI 框架：9 个 router、54 个业务端点 + `/api/health`，出入参由 Pydantic 校验，Swagger 文档自动生成（`/docs`）——**接口即文档**。
  - 上传、抽取这类阻塞动作走同步端点/线程池，不吃事件循环；响应统一经 `response_model` 裁剪，内部字段不会意外漏给前端。

- **Pydantic v2**：`schemas.py`（311 行）作为接口数据契约，校验 + 序列化 + 文档一次写完，省掉大量手写判断。

- **SQLAlchemy 2.0**：`models.py`（266 行）13 张表即代码；关联查询、级联删除、统计聚合都用 ORM 表达。数据库无关——`DATABASE_URL` 换成 Postgres 即可迁移。

- **PyMuPDF + openai SDK**

  - `PyMuPDF(fitz)` 解析 PDF 文本快而准，支撑「PDF → 分块 → 抽取」链路；Markdown / TXT 复用同一套分块逻辑，扩展名白名单在 `config.ALLOWED_EXTENSIONS`。
  - `openai` SDK 统一调用模型，`OPENAI_BASE_URL` 可指向任意 OpenAI 兼容端点（DeepSeek / 本地模型 / 中转），**不锁定厂商**。

## 三、数据库：SQLite

- 零部署、单文件（`data/knowledge_atlas.db`）：**备份 = 拷贝文件，迁移 = 移动文件**。
- `database.py` 在建连接时开好四个 PRAGMA：`foreign_keys=ON`、`journal_mode=WAL`、`busy_timeout=5000`、`synchronous=NORMAL` —— 一劳永逸地治住 SQLite 两个经典坑：级联删除静默失效、并发写报 `database is locked`。
- **升级不丢数据**：`ensure_schema()` 启动时把旧库补齐到当前 schema —— `ALTER TABLE` 补列、按需补索引、重建漂移的 `UNIQUE` 约束（`ALTER TABLE` 改不了约束，只能重建表并搬数据），并给历史库回填空缺的 `mention_count`。全程幂等，重复启动无副作用，`tests/test_migration.py` 专门覆盖。
- 单机、低频并发的场景下单文件数据库完全够用；真要上多用户高并发，替换 `DATABASE_URL` 后业务代码几乎不用动。

## 四、账号与安全

- **刻意零第三方依赖**：口令用标准库 `hashlib.pbkdf2_hmac(sha256)` 加盐派生（不存明文）；令牌是 `HMAC-SHA256` 签名的自包含串（载荷 + 签名），**服务端不需要会话表**；校验用 `hmac.compare_digest` 防时序侧信道。想换 bcrypt 只需改一个函数。
- `AUTH_SECRET` 缺失时自动生成并持久化到 `data/.auth_secret`，**重启不会把已登录用户踢下线**；令牌有效期 72 小时（`TOKEN_TTL_HOURS`）。
- 归属（`owner_id`）贯穿文档 / 实体 / 关系 / 证据 / 问答 / 撤销栈 / 空间 / 偏好：未登录只能读无主的公共数据，所有写接口一律需要登录。
- **密钥隔离**：模型密钥只存在于后端 `.env`，不进前端、不进版本库；连后端地址都由 Vite 代理隐藏。

## 五、AI 的边界与健壮性

- **AI 只做两件事**：从文本块里抽实体/关系、把证据组织成可读回答。检索、BFS 遍历、统计、去重校验、撤销全部由程序完成 —— 因此结果**可复现、可追踪、可撤销**。
- **离线兜底**：没有密钥或模型不可用时，抽取降级为规则启发式、问答降级为「证据清单式」回答，并在响应里标注 `model_available: false`。功能不缺，只是质量下降。
- **抽取管线的工程化**：
  - 分块（`CHUNK_MAX_CHARS` 默认 900 字/块）→ **线程池并发抽取**（`ThreadPoolExecutor`，`EXTRACT_CONCURRENCY` 默认 4；离线或显式设 1 时自动退化为串行，不做无谓开销）→ 白名单过滤 + 归一化去重后落库。
  - **进度不落库**：`services/progress.py` 用纯内存登记进度，因为抽取期间主请求握着数据库事务，进度查询一旦碰库就会和它抢 SQLite 写锁、把界面卡住。
  - **ETA 采用「先验 + 实测」两段式**：首块未完成时用典型值兜底，之后换成实测平均单块耗时，预估会随实际速度自动收敛。
  - **长任务先问后跑**：预计耗时超过 `EXTRACT_HEAVY_SECONDS`（默认 60 秒）时，前端先让用户选「完整抽取」还是「快速抽取」（只处理前 12 块），用覆盖面换时间。
- **调用模型时的容错**（`services/llm.py`）：
  - 默认带 `response_format={"type": "json_object"}` 强约束输出；端点报「不支持」时自动改为提示约束重试一次。
  - 推理模型会把「思考」计入 `max_tokens`：预算不足导致正文为空时自动翻倍重试，避免整块文档被降级成规则抽取。
  - 模型输出先清洗再解析（剥 ```json 围栏、去尾随逗号、JSON 被截断时按栈补齐 `}` 与 `]`），尽量从「模型话没说全」里救回结果。
  - 异常压缩成一句人话（去掉 `request_id` 等噪声）再展示，避免错误栏被原始 JSON 淹没。

## 六、整体架构优点

- **清晰分层**：`routers(接口) → services(业务) → models(ORM) → schemas(契约)`；17 个 service 各自单一职责（`pipeline` 抽取编排、`dedupe` 消歧、`trash` 撤销重做、`retrieval` RAG、`spaces` / `settings`、`insight` / `gaps`、`progress` 进度……），加功能基本是「新增一个 service + 挂一条路由」。
- **白名单思维**：关系类型必须落在 `RELATION_TYPES` 内（否则降级为 `related_to`），实体类型、上传扩展名同样白名单 —— 把「模型/用户乱来」挡在落库之前。
- **前后端分离**：前端是静态站，后端是单个 ASGI 进程，JSON 契约 + 可配 CORS 解耦，两端可独立部署与扩展。

## 七、总结

> **React 管交互 · FastAPI 管接口 · SQLite 管存储 · OpenAI SDK 管智能 · 程序管可追溯**

整体轻、可完全本地运行、离线也能演示完整链路。若未来面向多用户高并发，主要演进点是：把 `DATABASE_URL` 换成 Postgres、在 Uvicorn 前加反向代理、把抽取从线程池 + 内存进度升级为独立任务队列（当前形态对单机已足够）。
