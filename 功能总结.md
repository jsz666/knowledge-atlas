# 《知识星图》功能总结

把一堆论文、Markdown 笔记和课程讲义，变成一张可以沿着关系探索、每一步都有原文出处的知识星图。

---

## 一、核心闭环

```
文档上传 → 解析分块 → AI 抽取实体/关系(带来源证据)
        → 人工确认修正 → 图可视化探索 → 跨文档证据问答
```

**职责分工**（这套分工是系统的设计原则，不是实现细节）：

- **AI 负责**：实体关系抽取、问题中的关键概念识别、跨文档总结、每日洞察的措辞
- **程序负责**：分块、图遍历（BFS 最短路径）、来源追踪、证据组织、去重校验、统计与缺口分析

好处是：模型不可用时不"编造"，而是降级为规则抽取 + 证据清单，系统在离线状态下依然完整可用。

---

## 二、启动与访问

```bash
./start.sh                        # 后端 :8000 / 前端 :5173
VITE_PORT=5175 ./start.sh         # 指定前端端口
```

- 前端：`http://localhost:5173`（或指定端口）
- 接口文档：`http://localhost:8000/docs`
- 健康检查：`GET http://localhost:8000/api/health`（返回 `model_available` 与当前模型名）
- 日志：`tail -f /tmp/atlas_fe.log /tmp/atlas_be.log`
- 示例账号：`demo` / `demo12345`（也可点右上角「载入示例」一键灌入 4 篇文档 / 16 实体 / 18 关系，**无需 API key**）

未登录时可以浏览公共数据；上传、确认、合并、删除等写操作需要登录。

> 前后端是彻底解耦的：只要 `vite.config.js` 的代理指向后端端口，两端可以分别跑在不同环境里（例如前端跑在 Windows 侧、后端跑在 WSL 里），页面功能不受影响。

---

## 三、基础功能

### 文档管理

- 拖拽或选择上传，支持 **PDF / Markdown / TXT**（白名单 `.pdf / .md / .markdown / .txt`；扫描版 PDF 需先转成文字）
- 上传后自动完成解析、分块与抽取，侧栏显示处理状态
- **抽取进度可见**：进度条 + 已用时间 + 预计剩余。预计剩余先按典型速度估算，跑完若干块后改用实测速度，越跑越准
- **长文档先问再跑**：预计耗时超过 60 秒时先让你选「完整抽取」还是「快速抽取」（快速模式只处理前 12 块，用覆盖面换时间）
- **并发抽取**：默认 4 路并发（`EXTRACT_CONCURRENCY`），离线兜底或设为 1 时自动退回串行
- 抽取失败时把底层异常概括成一句可读原因显示在文档上，而不是把原始报错 JSON 丢给用户
- 可展开查看任意文档的**原文片段**，从证据回看上下文
- 支持**重新抽取**（改了抽取偏好后用它生效）、删除（连带清理片段与失去证据的实体）

### 知识图谱

- 共 **7 种布局**：力导向 fCoSE（默认）/ 经典 cose / 层次 ELK layered / 层次 breadthfirst / 环形 / 同心圆 / 网格
- ELK 布局引擎体积较大，只在真的切到它时才**按需加载**，不拖累首屏
- 按实体类型、按状态（已确认 / 待确认）筛选；可开关**边标签**与**按类型着色**
- 缩放、适应窗口、悬停 tooltip、点击查看详情
- 大图模式：超过 80 个节点时只渲染核心节点，**双击展开邻居**渐进探索
- 跨文档实体（出现在 2 篇以上文档）用紫色描边突出
- 最短路径：任选两个实体，程序 BFS 找出路径并在图上高亮

### 人工确认闭环

- AI 抽出的实体/关系默认是「待确认」，人工可改名、改类型、改描述或拒绝
- **实体消歧**：归一化识别重复实体（忽略大小写、空格、标点与"模型/方法"等后缀），合并后关系与证据自动迁移
- 合并时保留**旧名字作为别名**，用旧名字搜索仍能命中

### 撤销 / 重做

- 删除前自动存快照，支持单步撤销、跳序撤销（点某一条）、批量勾选撤销、按序重做
- 快捷键：`Ctrl/⌘+Z` 撤销、`Ctrl/⌘+Shift+Z` 重做、`/` 聚焦搜索、`Esc` 收起浮层

### 跨文档问答

- 提问后：AI 识别关键概念 → 程序锚定到图谱实体 → BFS 多跳遍历 → 取回原文证据 → AI 组织答案
- 答案附**关系路径**与**原文证据**，证据不足时会明说"现有资料不足以回答"
- 历史提问可回看，含概念、路径与证据快照

### 导入导出

- 导出：完整 JSON 快照（含原文片段）/ CSV 实体或关系清单 / Markdown 关系清单
- 导入：上传 JSON 快照合并回图谱，**幂等**（重复导入不产生重复数据）

---

## 四、账号与数据隔离

- 用户名 + 密码注册登录（密码用 pbkdf2 加盐哈希，不存明文）
- 令牌无状态，保存在浏览器本地，有效期 72 小时
- **数据完全按账号隔离**：文档、实体、关系、证据、问答、撤销栈、空间、偏好都带归属
- 未登录只能看无主的公共数据，所有写接口都需要登录
- 旧库平滑升级：新增字段通过迁移自动补列，**不会重建数据库、不丢数据**

---

## 五、个性化功能（10 项）

### 1. 个人仪表盘（我的主页）

登录后默认停留的页面。一次展示：

- 欢迎语 + 加入天数
- 六张数据卡：文档 / 实体 / 关系 / 跨文档实体 / 待确认 / 我的关注
- 待办提醒（带「去确认」快捷跳转）
- 我的关注、热门实体、最近上传、最近提问

### 2. 星标 / 关注实体

- 在节点详情标题旁点「☆ 关注」，或在我的主页、热门实体列表里点星标
- 星标实体在图谱上**金色粗描边**，一眼可见
- 图谱设置里有「**只看我关注的实体**」开关
- 切换星标**不会**改变实体的审核状态

### 3. 外观与视图记忆

- 顶栏一键切换**明/暗主题**，配色写入 `<html data-theme>` 并记在本地
- 图谱配色同步切换（节点、边、标签、高亮各有明暗两套）
- **视图偏好记忆**：布局方式（默认 fCoSE）、类型筛选、状态筛选、只看关注、边标签、按类型着色——改动即存，下次打开自动恢复

### 4. 知识空间

- 左侧空间切换器：「全部」+ 各空间（显示文档数 / 实体数），可新建、删除
- 每个文档下有归类下拉，随时移入 / 移出空间
- 选中空间后，**图谱、统计、搜索、待确认、问答**同步收敛
- 设计要点：实体不直接挂在空间下，而是通过「文档 → 证据 → 实体」间接推导，这样一个概念仍可跨空间复用
- 删除空间**不删文档**，文档只是回到「未归档」

### 5. 每日洞察

- 主页顶部卡片：汇总近 7 天新增、当前规模、待确认项、热门实体
- **按天缓存**，同一天只生成一次，不重复消耗模型调用
- 离线时用模板兜底，接入模型后变成自然语言摘要

### 6. 问答偏好

顶栏「设置」，四项偏好即时生效：

| 偏好   | 选项                           |
| ---- | ---------------------------- |
| 回答风格 | 简洁（要点式 200 字）/ 详尽（含推理 600 字） |
| 回答语言 | 中文 / English                 |
| 引用原文 | 逐条引用 / 只概括                   |
| 问答范围 | 全部文档 / 跟随当前空间                |

### 7. 自定义抽取维度

同一设置面板下方，三项配置（下次上传或「重抽」时生效）：

- **实体类型**：method / model / paper / person / tool / dataset / concept / org（不选 = 不限制）
- **关系类型**：related_to / derives_from / improves_on / part_of / uses / compared_with / proposed_by
- **关注领域**：一句话描述（如"Transformer 架构演进""产品功能与用户痛点"），注入抽取 prompt

过滤在**程序层兜底**——即使模型输出了不关注的维度，也会被丢弃；离线的规则抽取同样遵守设置。

### 8. 知识缺口提示

主页「知识缺口」区块，三类纯统计（不依赖模型）：

- **孤岛实体**：有原文支撑，却没有任何关系
- **单来源实体**：只在一篇文档出现过，证据偏弱
- **共现未连接**：两个实体经常一起出现，彼此却没有直接关系（最有价值的一类）

点击实体名可直接定位到图谱。

### 9. 只读分享

- 空间卡片上「分享」→ 生成令牌并自动复制链接：`http://localhost:5173/?share=<token>`
- 对方**无需登录**即可查看该空间的图谱、文档列表与统计
- 分享模式隐藏所有编辑入口；星标作为隐私**不对外暴露**
- 点「取消」立即撤销，旧链接马上失效（返回 404）

### 10. 交互细节

- 骨架屏加载态、Toast 通知、危险操作二次确认
- 空状态引导（未登录引导注册 / 已登录引导上传）
- 响应式侧栏与底部面板切换（节点详情 / 人工确认 / 问答 / 搜索结果 / 我的主页）

---

## 六、技术架构

| 层   | 技术                                                                                            |
| --- | --------------------------------------------------------------------------------------------- |
| 前端  | React 18 + Vite 5 + Cytoscape.js（fCoSE / ELK 布局引擎按需加载）                                      |
| 后端  | FastAPI + Uvicorn + SQLAlchemy 2 + SQLite（WAL / 外键强制开启 / busy_timeout），9 个 router、54 个业务端点 |
| 鉴权  | 标准库 `pbkdf2_hmac` 口令哈希 + HMAC-SHA256 自包含令牌（72 小时，服务端不存会话）                               |
| 抽取  | PyMuPDF 解析 → 分块（默认 900 字/块）→ 线程池并发抽取（默认 4 路）→ 白名单过滤 + 归一化去重 → 落库（全程带来源证据）          |
| 模型  | OpenAI 兼容接口（`OPENAI_BASE_URL` 可换任意端点），缺省或不可用时离线兜底                                                |
| 配置  | `backend/.env`（密钥不入库、不进前端）                                                                    |

**抽取管线与进度**：抽取是系统里唯一的长耗时操作，因此单独做了三件事——① 线程池并发压缩总耗时（离线或设为 1 路时自动退化为串行，不做无谓开销）；② 进度登记**纯内存、不查库**（`services/progress.py`），因为抽取期间主请求握着数据库事务，进度查询一旦碰库就会抢 SQLite 写锁把界面卡住；③ 进度里同时给出「已用时间 / 预计剩余」，预计值先按典型速度兜底、跑完首块后换成实测速度，估偏也能自我收敛。

**AI 与程序的边界**：模型只做"抽取"和"组织语言"，检索、遍历、统计、校验、撤销全部由程序完成，因此结果可复现、可追踪、可撤销。

**数据模型**（13 张表）：

```
users / documents / chunks / entities / relations / evidence
entity_aliases / qa_records / trash / spaces / user_settings / insights / shares
```

---

## 七、主要接口

| 分组   | 接口                                                                                                                                 |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 账号   | `POST /api/auth/register`、`/login`、`GET /me`、`POST /logout`                                                                        |
| 文档   | `GET/POST /api/documents`、`POST /upload`、`GET /progress`（抽取进度）、`GET /{id}/chunks`、`POST /{id}/extract`、`PATCH /{id}/space`、`DELETE /{id}`、`POST /import` |
| 图谱   | `GET /api/graph`、`/stats`、`/starred`、`/search`、`/review`、`/path`、`/duplicates`、`POST /merge`                                       |
| 实体关系 | `POST/PATCH/DELETE /api/graph/entities`、`/relations`                                                                               |
| 撤销   | `POST /api/graph/undo`、`/undo/{id}`、`/trash/undo-batch`、`/redo`、`GET/DELETE /trash`                                                |
| 问答   | `POST /api/qa/ask`、`GET /api/qa/records`、`/records/{id}`                                                                           |
| 个人   | `GET /api/me/dashboard`、`GET/PUT /api/me/settings`、`GET /api/me/insight`、`GET /api/me/gaps`                                           |
| 空间   | `GET/POST /api/spaces`、`PATCH/DELETE /{id}`、`GET/POST/DELETE /{id}/share`                                                          |
| 分享   | `GET /api/shared/{token}`（无需登录）                                                                                                    |
| 导出   | `/api/export/json`、`/csv`、`/markdown`                                                                                              |
| 开发   | `POST /api/dev/seed`、`/reset`、`GET /api/health`                                                                                    |

---

## 八、典型使用流程

1. 注册账号（或点右上角「载入示例」先看效果）
2. 上传同一主题的 5~10 篇文档
3. 到「人工确认」处理待确认的实体与关系，顺手合并重复实体
4. 用「知识空间」把文档按项目分组，分别查看各自的图谱
5. 把核心概念加星标，需要时开「只看我关注」
6. 到「跨文档问答」提问，答案会附带关系路径与原文出处
7. 定期看「我的主页」的每日洞察与知识缺口，补文档或补关系
8. 需要给别人看时，用空间的「分享」发一个只读链接

---

## 九、设计取舍

- **证据优先**：每个实体/关系都指回具体原文片段，宁可说"证据不足"也不编造
- **程序兜底**：模型只是增强项，离线时系统功能不缺（只是抽取质量下降）
- **隐私优先**：数据按账号完全隔离；分享是显式、可撤销、只读的
- **平滑升级**：表结构变更全部走启动迁移（补列 / 补索引 / 重建漂移的 `UNIQUE` 约束 / 回填冗余计数），老数据库直接升级，不重建、不丢数据
