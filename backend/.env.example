# 复制为 .env 后填写。.env 已在 .gitignore 中,不会被提交。
# 留空 OPENAI_API_KEY 也能运行,系统自动进入离线兜底模式。

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# 单个文档最多抽取的文本块数量,控制 token 消耗
MAX_CHUNKS_PER_DOC=40
